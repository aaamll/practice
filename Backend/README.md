# Backend (NestJS)

This is a minimal NestJS scaffold created for the project.

Quick start:

```bash
cd Backend
npm install
npm run start:dev
```

Environment variables are loaded from `.env`.

---

Boilerplate includes basic `AppModule`, `AuthModule`, and `UsersModule` (stubs).